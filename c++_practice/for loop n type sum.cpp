#include<stdio.h>
int main()
{
	/*Stop the program type 0*/
	double i,n,sum=0;
	for(i=0;i<=n;i++)
	{
		if(n!=0.0)
		{
		printf("Enter the renge :");
		scanf("%lf",&n);
		sum=sum+i;
	    }
	}
	printf("The sum of all num :-%2lf",sum);
	return 0;
}